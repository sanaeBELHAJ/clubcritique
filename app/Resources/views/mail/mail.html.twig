{% extends 'base.html.twig' %}

{% block title %}
   Envoyer un message
{% endblock %}

{% block body %}
<div class="row">
     <nav>
         <div class="nav-wrapper brown lighten-3">
               <div class="container">
                    <div class="col s12">
                      <a href="/reception" class="breadcrumb">Boite de reception</a>
                      <a href="{{path('messagerie_send',{'id':membreRecever.id})}}" class="breadcrumb">Nouveau message</a>
                    </div>
               </div>
            </div>
    </nav>
    <div class="container margin-top">
        <div class="row">
            <p>De  : moi</p>
            <p>à : <b>{{ membreRecever.prenom }} {{ membreRecever.nom }}</b> <span style="font-size:11px">({{ membreRecever.mail }})</span> </p>
            <fieldset>
                {{ form_start(form) }}
                    {{ form_rest(form) }}
                    <div class="right">
                         <button class="btn waves-effect waves-light" type="submit" name="action">Envoyer
                            <i class="material-icons right">send</i>
                          </button>
                    </div>
                {{ form_end(form) }}
            </fieldset>
        </div>
    </div>
</div>
    <br><br><br><br>
{% endblock %}
